{
  "compilerOptions": {
    /* Базовые настройки компиляции */
    "target": "ES2022",           // В какой стандарт JavaScript превращать код. ES2022 поддерживается всеми современными браузерами в 2025 году (включает работу с массивами и классами).
    "module": "ESNext",           // Определяет систему модулей. ESNext позволяет использовать современные import и export, которые понимает  сборщик esbuild.
    "lib": [                      // Список встроенных библиотек типов, которые TS будет знать "из коробки".
      "DOM",                      // Типы для работы с браузером (window, document).
      "DOM.Iterable",             // Позволяет перебирать элементы (например, результаты querySelectorAll) через forEach.
      "ESNext"                    // Самые свежие функции JavaScript (стандарты 2024-2025 годов).
    ],

    /* Совместимость и проверки */
    "allowJs": true,              // Разрешает подключать обычные .js файлы внутри  .ts файлов. Полезно для старых библиотек.
    "skipLibCheck": true,         // Ускоряет сборку: TS не будет проверять на ошибки файлы внутри папки node_modules.
    "esModuleInterop": true,      // Позволяет импортировать модули, написанные на старом формате CommonJS (через require), как обычные modern-модули.
    
    /* Строгость (Помощь разработчику) */
    "strict": true,               // Включает все проверки на ошибки: запрещает использовать переменные без типов и защищает от "undefined".
    
    /* Разрешение модулей (Как TS ищет файлы) */
    "moduleResolution": "node",  
    "isolatedModules": true       // Обязательная настройка для быстрых сборщиков (типа  esbuild). Она гарантирует, что каждый файл можно скомпилировать отдельно.
  },

  /* Где искать файлы для работы */
  "include": [
    "src/assets/ts/**/*"          // Указывает TS следить за всеми файлами с любым расширением внутри  папки с исходниками.
, "src/assets/ts/components/canvas.ts"  ]
}
